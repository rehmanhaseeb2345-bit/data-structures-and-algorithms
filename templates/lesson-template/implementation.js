/**
 * Lesson: <TOPIC NAME>
 * Module: <NN · Module name>
 *
 * Clean, commented reference implementation.
 * Run with:  node <path-to-this-file>
 */

'use strict';

// TODO: implement the concept here.
function example() {
  // ...
}

// --- Quick manual test -------------------------------------------------------
// Anything under this guard only runs when the file is executed directly,
// not when it is imported by another file.
if (require.main === module) {
  console.log('Running examples for: <TOPIC NAME>');
  // console.log(example(...));
}

module.exports = { example };
